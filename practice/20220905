# strip() : 공백, 줄바꿈 제거(white spcae)
# 함수 이름 지을 때, 낙타등처럼 대소문자 중간중간 섞어서 쓰면 좋음.

# 문자열은 변경 불가 -> 새로운 문자열을 만들어야 변경 가능
# 리스트는 변경 가능 -> 인덱스 연산자를 사용하여 리스트의 요소를 변경 가능
# 문자열과 리스트는 메소드가 비슷한데 split, strip 등 문자열에만 적용가능한 함수가 있다.
# 리스트 선언 필수 !
# 함수 선언 위에서 ! 변수, 리스트 사용 전 선언 및 초기화 !
# 리스트.sort() : 정렬 -> 영문자,숫자 상관없이 정렬가능 !
# 리스트.insert(index, 삽입할내용) : 특정 인덱스에 내용 삽입 !
y = [1,7,3,9,5]
y.sort()
print(y)

# swap
def swap(a,b):
    return b,a

a = 10
b = 20
print(swap(a,b))

class.method()
inner function()
